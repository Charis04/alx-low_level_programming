/**
 * mul - multiplies 2 ints
 * @a: first int to multiply
 * @b: int to be multiplied with first int
 * Return: product of multiplication
 */

int mul(int a, int b)
{
	int ret;

	ret = a * b;
	return (ret);
}
